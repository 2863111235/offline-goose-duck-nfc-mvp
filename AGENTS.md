# AGENTS.md

本文件适用于 `nfc-mvp/` 目录及其所有子目录。后续任何开发代理或开发者都必须先阅读并遵守本文件。

## 1. 当前阶段

- 用户已于 2026-09-12 批准进入 Phase 1：最小 MVP 实现与 NFC 现场验证。
- 只实现 `docs/MVP_SCOPE.md` 已列出的击杀、任务、冷却、身份选择和裁判控制。
- 新功能仍需用户明确批准，不因进入编码阶段而自动扩大范围。

## 2. 必读资料与优先级

开始实现前依次阅读仓库内资料：

1. `docs/MVP_SCOPE.md`；
2. `docs/ENVIRONMENT.md`。

本地上级工作区若存在 `NFC原理与V1名牌击杀方案.md` 和 `offline-goose-duck-v1-execution-framework-current (1).md`，可作为背景资料阅读，但不得让仓库依赖这些外部文件才能开发或运行。

若规则冲突，按以下优先级处理：用户当前明确指示 > `docs/MVP_SCOPE.md` 中已确认的 MVP 决策 > NFC 方案文档 > 旧执行框架。不得自行把“待商榷”写成固定规则；应做成配置或记录为待确认项。

## 3. 产品边界

本项目是线下社交游戏的辅助裁判系统，不是把整局游戏搬到手机里。

线上仅处理必须精确、容易争议或需要即时同步的内容：身份会话、标签绑定、击杀判定、冷却计时、任务记录、阶段开关、状态同步和审计日志。交流、移动、实体任务过程、尸体标记、公聊及现场裁决继续在线下完成。

第一版只验证击杀与任务两个 NFC 闭环。不得擅自加入投票、完整角色技能、夜间流程、聊天、支付、排行榜、公开账号体系、原生 App 或小程序。

## 4. NFC 与 iPhone 约束

- 标签使用普通 NTAG213，写入 NDEF URL；不依赖底层 UID，也不把标签当作安全密钥。
- iPhone 网页方案依赖系统识别 URL 后由玩家点开通知；不得声称 Safari 可以静默扫描 NFC。
- NFC URL 只包含不可猜测的标签令牌，例如 `/nfc/player/<token>` 或 `/nfc/task/<token>`。
- 标签中不得写入角色、阵营、姓名、存活状态、冷却、数据库主键顺序或任何管理权限。
- 静态标签可以被复制或重放。MVP 必须明确这是受监督测试方案，并由登录会话、场次绑定、阶段、状态版本、幂等键和服务端事务降低误操作；不得把它描述为强防作弊方案。

## 5. 服务端为最终裁判

所有关键规则必须在服务端重新校验，前端倒计时和按钮禁用只能用于提示。

一次击杀至少校验：

1. 操作者已进入当前场次且仍存活；
2. 操作者拥有击杀权限；
3. 当前阶段允许击杀；
4. 目标标签属于当前场次；
5. 目标仍存活，且不是操作者本人；
6. 阵营限制符合当前规则配置；
7. 操作者的个人 5 分钟击杀冷却已结束；
8. 同一请求未被重复结算。

击杀成功时，目标死亡、击杀者冷却起点和事件日志必须在同一事务中提交。并发请求只能有一个成功结果。

一次任务操作至少校验：当前场次、任务点绑定、玩家存活、当前阶段、任务状态、该玩家对同一任务的个人 3 分钟重复冷却以及请求幂等性。实体任务是否完成由第一版的裁判确认动作决定，不由 NFC 碰触本身决定。

服务端时间是唯一可信时间。数据库保存时间点，前端根据服务端时间展示剩余秒数，不保存独立真相。

## 6. 拟议技术基线

当前已经确认的技术基线：

- Node.js 24 + TypeScript；
- Next.js 单体全栈应用；
- SQLite 单机数据库；
- npm；
- 单一对外端口，监听 `0.0.0.0`；
- 轮询作为 MVP 状态同步方式，暂不引入 WebSocket；
- 简单本局会话，不接入第三方登录。

数据库使用 `better-sqlite3`。其当前版本声明支持 Node 22 以上；若未来升级失败，不得静默更换运行时、数据库驱动或包管理器。

## 7. 建议的代码边界

选型批准后，应保持以下逻辑边界，即使框架的实际目录名不同：

- `domain`：阶段、玩家、击杀、任务、冷却和状态转换；不得依赖 UI。
- `server`：路由、会话、鉴权、数据库事务和时间来源。
- `web`：玩家页、NFC 落地页、裁判页和倒计时展示。
- `tests`：领域规则、并发/幂等、接口和关键手机流程。
- `data`：本地 SQLite 文件；真实数据库不得提交版本库。

不要提前拆分微服务、共享包、消息队列或复杂状态管理。只有在一个可验证需求确实需要时才增加依赖。

## 8. 实施顺序

用户批准写代码后按以下顺序推进：

1. 冻结数据模型、状态机和接口契约；
2. 实现数据库迁移与种子测试场次；
3. 实现服务端击杀事务和任务冷却；
4. 实现最小裁判页；
5. 实现玩家进入页及 NFC 落地页；
6. 自动化测试并模拟重复提交和并发击杀；
7. 电脑局域网访问测试；
8. 写入少量 NTAG213，在 iPhone 和安卓上实测。

每一步只实现 `docs/MVP_SCOPE.md` 已列出的内容。发现规则未定时，先记录假设并请求确认，不得扩展产品范围。

## 9. 验收和质量要求

- 关键领域规则必须有自动化测试；测试使用可控时钟，不能真的等待 3 或 5 分钟。
- 必须覆盖重复点击、浏览器刷新、旧链接、目标已死亡、错误阶段、冷却未结束和两名玩家同时击杀同一目标。
- 所有管理修正必须留下追加式事件记录，包括操作者、时间、原因和前后状态。
- 页面首先按手机竖屏和弱网场景设计；结果必须用明确文字与颜色表达，不依赖声音或震动作为唯一反馈。
- 不记录不必要的个人信息，不在日志中输出会话密钥或完整管理令牌。
- 实现后至少验证：本机测试、局域网手机访问、iPhone NFC URL 打开、安卓 NFC URL 打开。

## 10. 文件与变更纪律

- 保持提交小而可解释，不修改根目录中的原始需求文档，除非用户明确要求。
- `.env`、数据库文件、日志、构建产物和依赖目录不得提交；`.env.example` 只放无敏感信息的示例。
- 不覆盖用户已有文件，不清理与当前任务无关的内容。
- 修改规则、接口或环境约定时，同步更新相应文档。
- 完成任务前删除临时文件和调试产物，只保留用户需要的交付物。

<!-- BEGIN:nextjs-agent-rules -->

# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` (resolved from this file's directory; in monorepos the `next` package may not be visible from the repo root) before writing any code. Heed deprecation notices.

This block is written and re-added by `next dev` — verify at `node_modules/next/dist/server/lib/generate-agent-files.js`. Removing it from a diff only re-creates the uncommitted change; committing it with your work keeps the tree clean.

<!-- END:nextjs-agent-rules -->
